{
  "name": "generated-afdbde4e6861103b6e275de1",
  "scripts": {
    "dev": "wrangler dev src/index.ts",
    "deploy": "wrangler deploy --minify src/index.ts",
    "db:touch": "wrangler d1 execute fact_checker --local --command='SELECT 1'",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "wrangler d1 migrations apply fact_checker --local",
    "db:migrate:prod": "ENVIRONMENT=production drizzle-kit migrate",
    "db:setup": "npm run db:touch && npm run db:generate && npm run db:migrate",
    "db:studio": "drizzle-kit studio",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fiberplane/hono": "0.5.5",
    "@hono/mcp": "0.1.1",
    "@modelcontextprotocol/sdk": "1.17.1",
    "dotenv": "^17.2.1",
    "drizzle-orm": "^0.44.3",
    "hono": "4.8.5",
    "zod": "3.25.76"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20250321.0",
    "@libsql/client": "0.14.0",
    "drizzle-kit": "0.30.4",
    "tsx": "4.19.2",
    "wrangler": "^4.4.0"
  }
}
